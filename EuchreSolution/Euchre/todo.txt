Next
- Validate hooman actions nicely
- Make animations actually timed properly and probably factor out all the visual stuff so it doesn't suck
- Move card being ordered up to dealer's hand before waiting for a discard
- Typescriptify anim

Bugs


ToDo
- Fix decent AI problems
- Move message printing and animations to the controller
- Make html look less dumb
- Implement defend alone
- AI difficulties
- Rename decentAI to something less flattering
- make sure dealer carries over between games


Clean up
- Validate params
- Use calc css
- Fix up sidebar

Enhancements
- Make it clear when it's your turn
- Cats settings
- Improve UI
- Queue up your next card
	- Get rid of delay at start of player 0's turn
- Recenter hand after cards are played
- If your partner is going alone, you get to see the kitty (meow)
- Speedy (instant) AI players
- Auto-play last trick
- Auto-pass if it's all you can do
- Multiplayer
- Card pack choice
- Make the whole thing resize with window

Low priority game modes
- Stick the dealer
- AIs can see open hands but you can't. No one will find this fun (except Kevin). Do it anyway.
- Game mode where you try to lose

---Reminders---

Keyboard stuff
- ctrl + shift click (go to definition)
- ctrl + shift + right click (go back)



let message = `${Player[bidResult.maker]} `;
message += `ordered up the ${Rank[this.__trumpCandidate.rank]} of ${Suit[bidResult.trump]}`;
message += `called ${Suit[bidResult.trump]}`;
message += " (alone)";
animShowText(message, MessageLevel.Step, 1);

animShowText(`${player} passed.`, MessageLevel.Step, 1);
